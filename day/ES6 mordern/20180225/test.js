function getValue(condition){

    var value;
    if(condition){
        value = "blue";
        //나머지 코드
        return value;
    }else{
        // value는 여기서 undefined로 존재한다.
        return null;
    }
    // value는 여기서 undefined로 존재한다.
}